# Core analysis stack (needed for the failure-analysis framework + mock backend)
numpy>=1.23
opencv-python>=4.7
scikit-image>=0.20
matplotlib>=3.6
pandas>=1.5
PyYAML>=6.0

# Real DDColor model backend
torch>=1.13
torchvision>=0.14
huggingface_hub>=0.20
timm>=0.9
# The official DDColor repo uses a BasicSR training pipeline. basicsr provides
# the architecture registry the model class relies on.
basicsr>=1.4.2

# Optional: modelscope offers an alternative one-line pipeline for DDColor.
# Uncomment if you prefer the ModelScope loading path.
# modelscope>=1.9
